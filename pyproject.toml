[project]
name = "clouddash-support"
version = "0.1.0"
description = "Multi-agent customer support reference implementation for CloudDash"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
